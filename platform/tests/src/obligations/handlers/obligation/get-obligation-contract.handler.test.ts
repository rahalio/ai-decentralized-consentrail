/**
 * GetObligationContract handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: obligations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getObligationContract } from "@consentrail/api-server/domains/obligations/handlers";
import type { ObligationsDDDDependencies } from "@consentrail/api-server/domains/obligations";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@consentrail/tests/shared";
import { OBLIGATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetObligationContract", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ObligationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ObligationsDDDDependencies>(
      OBLIGATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetObligationContract, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { contractId: entity.id };

    await getObligationContract(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetObligationContract.execute).toHaveBeenCalledWith(expect.objectContaining({ contractId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { contractId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetObligationContract, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getObligationContract(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
