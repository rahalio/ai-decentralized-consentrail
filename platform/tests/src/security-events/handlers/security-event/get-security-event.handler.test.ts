/**
 * GetSecurityEvent handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: security-events
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getSecurityEvent } from "@consentrail/api-server/domains/security-events/handlers";
import type { SecurityEventsDDDDependencies } from "@consentrail/api-server/domains/security-events";
import { createSecurityEvent as createSecurityEventEntity } from "@consentrail/factories/security-events";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@consentrail/tests/shared";
import { SECURITYEVENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetSecurityEvent", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SecurityEventsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SecurityEventsDDDDependencies>(
      SECURITYEVENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createSecurityEventEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetSecurityEvent, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { eventId: entity.id };

    await getSecurityEvent(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetSecurityEvent.execute).toHaveBeenCalledWith(expect.objectContaining({ eventId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { eventId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetSecurityEvent, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getSecurityEvent(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
