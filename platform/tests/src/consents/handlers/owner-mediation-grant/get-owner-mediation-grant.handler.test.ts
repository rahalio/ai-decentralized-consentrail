/**
 * GetOwnerMediationGrant handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: consents
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getOwnerMediationGrant } from "@consentrail/api-server/domains/consents/handlers";
import type { ConsentsDDDDependencies } from "@consentrail/api-server/domains/consents";
import { createOwnerMediationGrant as createOwnerMediationGrantEntity } from "@consentrail/factories/consents";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@consentrail/tests/shared";
import { CONSENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetOwnerMediationGrant", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ConsentsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ConsentsDDDDependencies>(
      CONSENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createOwnerMediationGrantEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetOwnerMediationGrant, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { grantId: entity.id };

    await getOwnerMediationGrant(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetOwnerMediationGrant.execute).toHaveBeenCalledWith(expect.objectContaining({ grantId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { grantId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetOwnerMediationGrant, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getOwnerMediationGrant(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
