/**
 * RevokeRegulatorView handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: regulator-views
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { revokeRegulatorView } from "@consentrail/api-server/domains/regulator-views/handlers";
import type { RegulatorViewsDDDDependencies } from "@consentrail/api-server/domains/regulator-views";
import { createCreateRevokeRequest } from "@consentrail/factories/regulator-views";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@consentrail/tests/shared";
import { REGULATORVIEWS_USE_CASES } from "../__factories__/use-cases.js";

describe("RevokeRegulatorView", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: RegulatorViewsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<RegulatorViewsDDDDependencies>(
      REGULATORVIEWS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateRevokeRequest();
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeRevokeRegulatorView, "execute").mockResolvedValue(useCaseResult);

    await revokeRegulatorView(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeRevokeRegulatorView.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
