/**
 * GetIntegrityProof handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evidence
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getIntegrityProof } from "@consentrail/api-server/domains/evidence/handlers";
import type { EvidenceDDDDependencies } from "@consentrail/api-server/domains/evidence";
import { createIntegrityProof as createIntegrityProofEntity } from "@consentrail/factories/evidence";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@consentrail/tests/shared";
import { EVIDENCE_USE_CASES } from "../__factories__/use-cases.js";

describe("GetIntegrityProof", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: EvidenceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<EvidenceDDDDependencies>(
      EVIDENCE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createIntegrityProofEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetIntegrityProof, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { proofId: entity.id };

    await getIntegrityProof(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetIntegrityProof.execute).toHaveBeenCalledWith(expect.objectContaining({ proofId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { proofId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetIntegrityProof, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getIntegrityProof(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
