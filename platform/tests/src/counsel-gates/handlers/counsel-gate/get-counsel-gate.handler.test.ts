/**
 * GetCounselGate handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: counsel-gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getCounselGate } from "@consentrail/api-server/domains/counsel-gates/handlers";
import type { CounselGatesDDDDependencies } from "@consentrail/api-server/domains/counsel-gates";
import { createCounselGate as createCounselGateEntity } from "@consentrail/factories/counsel-gates";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@consentrail/tests/shared";
import { COUNSELGATES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetCounselGate", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CounselGatesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CounselGatesDDDDependencies>(
      COUNSELGATES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createCounselGateEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetCounselGate, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { gateId: entity.id };

    await getCounselGate(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetCounselGate.execute).toHaveBeenCalledWith(expect.objectContaining({ gateId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { gateId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetCounselGate, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getCounselGate(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
