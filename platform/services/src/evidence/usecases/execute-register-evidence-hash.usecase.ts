/**
 * ExecuteRegisterEvidenceHash - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evidence
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRegisterEvidenceHash Use Case
 *
 * DDD: Application use case for create Hash.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RegisterEvidenceHashInput, RegisterEvidenceHashOutput } from "../dto/hash.dto";
import type { ExecutionContextService, IdGeneratorService } from "@consentrail/services/_shared/index.js";
import type { HashRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanCreate } from "../policies/can-create.policy";
export class ExecuteRegisterEvidenceHash {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly hash: HashRepository
  ) {}

  async execute(input: RegisterEvidenceHashInput): Promise<RegisterEvidenceHashOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.evdId();

    // All field names extracted from OpenAPI spec: containsPersonalData, hash, purpose, relatedCaseId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!input.hash) {
      throw new ValidationError("hash is required");
    }    // Check policy
    if (!CanCreate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Generate entity ID in services layer using IdGeneratorService (evidence domain)
    const entityId = this.idGenerator.evdId();

    // Create entity via repository (pass ID and correlation ID for response meta)
    const entity = await this.hash.registerEvidenceHash({ ...input, id: entityId, createdByActorId: (input as any).createdByActorId ?? this.context.getUserId(), createdByActorType: (input as any).createdByActorType ?? "human", orgId: this.context.getOrgId(), correlationId } as any);

    return entity as RegisterEvidenceHashOutput;
  }
}
