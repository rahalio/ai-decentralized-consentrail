/**
 * ExecuteIssueIntegrityProof - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evidence
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteIssueIntegrityProof Use Case
 *
 * DDD: Application use case for get IntegrityProof.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { IssueIntegrityProofInput, IssueIntegrityProofOutput } from "../dto/integrity-proof.dto";
import type { ExecutionContextService, IdGeneratorService } from "@consentrail/services/_shared/index.js";
import type { IntegrityProofRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteIssueIntegrityProof {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly integrityProof: IntegrityProofRepository
  ) {}

  async execute(input: IssueIntegrityProofInput): Promise<IssueIntegrityProofOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.evdId();

    // All field names extracted from OpenAPI spec: artefactRef
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.integrityProof.issueIntegrityProof({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as IssueIntegrityProofOutput;
  }
}
