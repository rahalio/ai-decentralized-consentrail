/**
 * ExecuteAssignConsortiumRole - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: consortium-roles
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAssignConsortiumRole Use Case
 *
 * DDD: Application use case for get ConsortiumRole.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AssignConsortiumRoleInput, AssignConsortiumRoleOutput } from "../dto/consortium-role.dto";
import type { ExecutionContextService, IdGeneratorService } from "@consentrail/services/_shared/index.js";
import type { ConsortiumRoleRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAssignConsortiumRole {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly consortiumRole: ConsortiumRoleRepository
  ) {}

  async execute(input: AssignConsortiumRoleInput): Promise<AssignConsortiumRoleOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cnrId();

    // All field names extracted from OpenAPI spec: contactRef, kind, memberId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).memberId) {
      throw new ValidationError("memberId is required");
    }
    const memberId = (input as any).memberId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.consortiumRole.assignConsortiumRole({
      ...input,
      memberId,
      orgId: this.context.getOrgId(),
      correlationId,
    } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${memberId}`);
    }

    return entity as AssignConsortiumRoleOutput;
  }
}
