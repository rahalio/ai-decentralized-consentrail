/**
 * RegulatorViews DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: regulator-views
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * RegulatorViews Domain Module - Composition root
 *
 * DDD: Composition root for regulator-views domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  RegulatorViewRepositoryAdapter,
  RevokeRepositoryAdapter,
} from "@consentrail/adapters/regulator-views";
import { getIdGeneratorService } from "@consentrail/adapters";
import type { AdapterDynamoDBClient } from "@consentrail/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetRegulatorView,
  ExecuteGrantRegulatorView,
  ExecuteListRegulatorViews,
  ExecuteRevokeRegulatorView,
} from "@consentrail/services/regulator-views/usecases";
// Ports (interfaces)
import type {
  RegulatorViewRepository,
  RevokeRepository,
} from "@consentrail/services/regulator-views/ports";

export interface RegulatorViewsDomainModule {
  repos: {
    regulatorViews: RegulatorViewRepository;
    revokes: RevokeRepository;
  };
  useCases: {
    regulatorViews: {
      create: ExecuteGrantRegulatorView;
      get: ExecuteGetRegulatorView;
      list: ExecuteListRegulatorViews;
    };
    revokes: {
      get: ExecuteRevokeRegulatorView;
    };
  };
}

export function buildRegulatorViewsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): RegulatorViewsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      regulatorViews?: RegulatorViewRepository;
      revokes?: RevokeRepository;
    } = {};

    obj.regulatorViews = new RegulatorViewRepositoryAdapter(dynamoClient);
    obj.revokes = new RevokeRepositoryAdapter(dynamoClient);
    return {
      regulatorViews: obj.regulatorViews!,
      revokes: obj.revokes!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    regulatorViews: {
      create: new ExecuteGrantRegulatorView(executionContext, idGenerator, repos.regulatorViews),
      get: new ExecuteGetRegulatorView(executionContext, idGenerator, repos.regulatorViews),
      list: new ExecuteListRegulatorViews(executionContext, idGenerator, repos.regulatorViews),
    },
    revokes: {
      get: new ExecuteRevokeRegulatorView(executionContext, idGenerator, repos.revokes),
    },
  };
  return { repos, useCases: useCases as RegulatorViewsDomainModule["useCases"] };
}
