/**
 * Consents DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: consents
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Consents Domain Module - Composition root
 *
 * DDD: Composition root for consents domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ApproveRepositoryAdapter,
  ConsentRepositoryAdapter,
  DenyRepositoryAdapter,
  OwnerMediationGrantRepositoryAdapter,
  ProcessingStopRepositoryAdapter,
  WithdrawRepositoryAdapter,
} from "@consentrail/adapters/consents";
import { getIdGeneratorService } from "@consentrail/adapters";
import type { AdapterDynamoDBClient } from "@consentrail/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteApproveOwnerMediationGrant,
  ExecuteCaptureConsent,
  ExecuteDenyOwnerMediationGrant,
  ExecuteGetConsentRecord,
  ExecuteGetOwnerMediationGrant,
  ExecuteGetProcessingStopState,
  ExecuteListConsentRecords,
  ExecuteListOwnerMediationGrants,
  ExecuteRequestOwnerMediationGrant,
  ExecuteRetryProcessingStopPush,
  ExecuteWithdrawConsent,
} from "@consentrail/services/consents/usecases";
// Ports (interfaces)
import type {
  ApproveRepository,
  ConsentRepository,
  DenyRepository,
  OwnerMediationGrantRepository,
  ProcessingStopRepository,
  WithdrawRepository,
} from "@consentrail/services/consents/ports";

export interface ConsentsDomainModule {
  repos: {
    approves: ApproveRepository;
    consents: ConsentRepository;
    denies: DenyRepository;
    ownerMediationGrants: OwnerMediationGrantRepository;
    processingStops: ProcessingStopRepository;
    withdraws: WithdrawRepository;
  };
  useCases: {
    approves: {
      create: ExecuteApproveOwnerMediationGrant;
    };
    consents: {
      create: ExecuteCaptureConsent;
      get: ExecuteGetConsentRecord;
      list: ExecuteListConsentRecords;
    };
    denies: {
      get: ExecuteDenyOwnerMediationGrant;
    };
    ownerMediationGrants: {
      create: ExecuteRequestOwnerMediationGrant;
      get: ExecuteGetOwnerMediationGrant;
      list: ExecuteListOwnerMediationGrants;
    };
    processingStops: {
      get: ExecuteGetProcessingStopState;
      retry: ExecuteRetryProcessingStopPush;
    };
    withdraws: {
      get: ExecuteWithdrawConsent;
    };
  };
}

export function buildConsentsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ConsentsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      approves?: ApproveRepository;
      consents?: ConsentRepository;
      denies?: DenyRepository;
      ownerMediationGrants?: OwnerMediationGrantRepository;
      processingStops?: ProcessingStopRepository;
      withdraws?: WithdrawRepository;
    } = {};

    obj.approves = new ApproveRepositoryAdapter(dynamoClient);
    obj.consents = new ConsentRepositoryAdapter(dynamoClient);
    obj.denies = new DenyRepositoryAdapter(dynamoClient);
    obj.ownerMediationGrants = new OwnerMediationGrantRepositoryAdapter(dynamoClient);
    obj.processingStops = new ProcessingStopRepositoryAdapter(dynamoClient);
    obj.withdraws = new WithdrawRepositoryAdapter(dynamoClient);
    return {
      approves: obj.approves!,
      consents: obj.consents!,
      denies: obj.denies!,
      ownerMediationGrants: obj.ownerMediationGrants!,
      processingStops: obj.processingStops!,
      withdraws: obj.withdraws!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    approves: {
      create: new ExecuteApproveOwnerMediationGrant(executionContext, idGenerator, repos.approves),
    },
    consents: {
      create: new ExecuteCaptureConsent(executionContext, idGenerator, repos.consents),
      get: new ExecuteGetConsentRecord(executionContext, idGenerator, repos.consents),
      list: new ExecuteListConsentRecords(executionContext, idGenerator, repos.consents),
    },
    denies: {
      get: new ExecuteDenyOwnerMediationGrant(executionContext, idGenerator, repos.denies),
    },
    ownerMediationGrants: {
      create: new ExecuteRequestOwnerMediationGrant(executionContext, idGenerator, repos.ownerMediationGrants),
      get: new ExecuteGetOwnerMediationGrant(executionContext, idGenerator, repos.ownerMediationGrants),
      list: new ExecuteListOwnerMediationGrants(executionContext, idGenerator, repos.ownerMediationGrants),
    },
    processingStops: {
      get: new ExecuteGetProcessingStopState(executionContext, idGenerator, repos.processingStops),
      retry: new ExecuteRetryProcessingStopPush(executionContext, idGenerator, repos.processingStops),
    },
    withdraws: {
      get: new ExecuteWithdrawConsent(executionContext, idGenerator, repos.withdraws),
    },
  };
  return { repos, useCases: useCases as ConsentsDomainModule["useCases"] };
}
