/**
 * System Routes - Health check and routes listing
 *
 * AUTO-GENERATED by codegen (api_server_system_routes). Do not edit by hand.
 * Domains and routeExamples are derived from discovered api-server domains and
 * each domain's OpenAPI bundled spec.
 */

import type { FastifyInstance } from "fastify";
import {
  getCoreTableName,
  getBaseTableName,
  getAnalyticsTableName,
} from "@consentrail/adapters";
import { getRegisteredRoutes } from "./route-registry.js";

/**
 * Register system routes (health check, routes listing)
 */
export function registerSystemRoutes(fastify: FastifyInstance): void {
  // Health check (public - no auth required)
  fastify.get("/health", { config: { scope: "public" } }, async () => {
    try {
      const coreTable = getCoreTableName();
      const baseTable = getBaseTableName();
      const analyticsTable = getAnalyticsTableName();
      return {
        status: "ok",
        database: "dynamodb",
        tables: {
          core: coreTable,
          base: baseTable,
          analytics: analyticsTable,
        },
      };
    } catch (error: unknown) {
      const errorMessage =
        error instanceof Error ? error.message : String(error);
      fastify.log.warn({ error: errorMessage }, "Health check failed");
      return { status: "error", database: "disconnected", error: errorMessage };
    }
  });

  // Routes listing endpoint (public)
  fastify.get("/routes", { config: { scope: "public" } }, async () => {
    const domains = [
      "consents", "consortium-roles", "counsel-gates", "evidence", "identity", "obligations", "regulator-views", "rights", "security-events", "triple-blind-exchange"
    ];

    const routeExamples: Record<
      string,
      Array<{ method: string; path: string; description?: string }>
    > = {
      consents: [{ method: "GET", path: "/v1/consents" }, { method: "POST", path: "/v1/consents" }, { method: "GET", path: "/v1/consents/:consentId" }, { method: "POST", path: "/v1/consents/:consentId/withdraw" }, { method: "GET", path: "/v1/consents/:consentId/processing-stop" }, { method: "POST", path: "/v1/consents/:consentId/processing-stop" }, { method: "GET", path: "/v1/owner-mediation-grants" }, { method: "POST", path: "/v1/owner-mediation-grants" }, { method: "GET", path: "/v1/owner-mediation-grants/:grantId" }, { method: "POST", path: "/v1/owner-mediation-grants/:grantId/approve" }, { method: "POST", path: "/v1/owner-mediation-grants/:grantId/deny" }],
      "consortium-roles": [{ method: "GET", path: "/v1/consortium-roles" }, { method: "POST", path: "/v1/consortium-roles" }, { method: "GET", path: "/v1/consortium-roles/:roleId" }, { method: "POST", path: "/v1/consortium-roles/:roleId/attest" }, { method: "GET", path: "/v1/consortium-roles/go-live-gate" }],
      "counsel-gates": [{ method: "GET", path: "/v1/counsel-gates" }, { method: "POST", path: "/v1/counsel-gates" }, { method: "GET", path: "/v1/counsel-gates/:gateId" }, { method: "POST", path: "/v1/counsel-gates/:gateId/decide" }],
      evidence: [{ method: "GET", path: "/v1/evidence/hashes" }, { method: "POST", path: "/v1/evidence/hashes" }, { method: "GET", path: "/v1/evidence/hashes/:hashId" }, { method: "POST", path: "/v1/evidence/hashes/:hashId/orphan" }, { method: "GET", path: "/v1/evidence/integrity-proofs" }, { method: "POST", path: "/v1/evidence/integrity-proofs" }, { method: "GET", path: "/v1/evidence/integrity-proofs/:proofId" }, { method: "POST", path: "/v1/evidence/integrity-proofs/:proofId/verify" }],
      identity: [{ method: "GET", path: "/v0/tenants/me/api-keys" }, { method: "POST", path: "/v0/tenants/me/api-keys" }, { method: "GET", path: "/v0/tenants/me/api-keys/:keyId" }, { method: "DELETE", path: "/v0/tenants/me/api-keys/:keyId" }, { method: "GET", path: "/v0/tenants/me/users" }, { method: "POST", path: "/v0/tenants/me/users" }, { method: "GET", path: "/v0/tenants/me/users/:userId" }, { method: "PATCH", path: "/v0/tenants/me/users/:userId" }, { method: "POST", path: "/v0/tenants/me/users/:userId/disable" }, { method: "POST", path: "/v0/tenants/me/users/:userId/enable" }, { method: "POST", path: "/v0/auth/login" }, { method: "GET", path: "/v0/auth/me" }, { method: "PATCH", path: "/v0/auth/me" }, { method: "POST", path: "/v0/auth/refresh" }, { method: "POST", path: "/v0/auth/logout" }],
      obligations: [{ method: "GET", path: "/v1/obligations" }, { method: "POST", path: "/v1/obligations" }, { method: "GET", path: "/v1/obligations/:contractId" }, { method: "POST", path: "/v1/obligations/:contractId/evaluate" }, { method: "GET", path: "/v1/compliance-alerts" }, { method: "POST", path: "/v1/compliance-alerts/:alertId/acknowledge" }, { method: "POST", path: "/v1/compliance-alerts/:alertId/waive" }],
      "regulator-views": [{ method: "GET", path: "/v1/regulator-views" }, { method: "POST", path: "/v1/regulator-views" }, { method: "GET", path: "/v1/regulator-views/:viewId" }, { method: "POST", path: "/v1/regulator-views/:viewId/revoke" }],
      rights: [{ method: "GET", path: "/v1/rights-cases" }, { method: "POST", path: "/v1/rights-cases" }, { method: "GET", path: "/v1/rights-cases/:caseId" }, { method: "POST", path: "/v1/rights-cases/:caseId/close" }, { method: "POST", path: "/v1/rights-cases/:caseId/hand-to-counsel" }],
      "security-events": [{ method: "GET", path: "/v1/security-events" }, { method: "POST", path: "/v1/security-events" }, { method: "GET", path: "/v1/security-events/:eventId" }, { method: "POST", path: "/v1/security-events/:eventId/acknowledge" }],
      "triple-blind-exchange": [{ method: "GET", path: "/v1/triple-blind-policies" }, { method: "POST", path: "/v1/triple-blind-policies" }, { method: "GET", path: "/v1/triple-blind-policies/:policyId" }, { method: "POST", path: "/v1/triple-blind-policies/:policyId/enable" }, { method: "POST", path: "/v1/triple-blind-policies/:policyId/disable" }, { method: "POST", path: "/v1/triple-blind-policies/:policyId/dry-run" }],
    };

    return {
      baseUrl: "http://localhost:4000",
      routePattern: "/orgs/:orgId/{domain}/{resource}",
      authentication:
        "All routes (except /health and /routes) require JWT token in Authorization header",
      domains: domains.sort(),
      routeExamples,
      systemRoutes: [
        { method: "GET", path: "/health", description: "Health check endpoint" },
        { method: "GET", path: "/routes", description: "List available routes" },
      ],
      note: "Replace :orgId with actual organization ID. All domain routes require authentication.",
    };
  });

  // Full route list for API tester page (public)
  fastify.get("/routes/full", { config: { scope: "public" } }, async () => {
    const routes = getRegisteredRoutes();
    const byDomain: Record<
      string,
      Array<{ method: string; path: string }>
    > = {};
    const system: Array<{ method: string; path: string }> = [];
    for (const r of routes) {
      const m = r.path.match(/^\/orgs\/:orgId\/([^/]+)/);
      if (m) {
        const domain = m[1];
        if (!byDomain[domain]) byDomain[domain] = [];
        byDomain[domain].push(r);
      } else {
        system.push(r);
      }
    }
    return {
      baseUrl: "http://localhost:4000",
      total: routes.length,
      system,
      byDomain: Object.keys(byDomain)
        .sort()
        .reduce(
          (acc, k) => ({ ...acc, [k]: byDomain[k] }),
          {} as Record<string, Array<{ method: string; path: string }>>,
        ),
    };
  });
}
