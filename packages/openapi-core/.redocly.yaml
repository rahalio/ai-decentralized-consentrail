apis:
  identity:
    root: src/identity.yaml
  consents:
    root: src/consents.yaml
  rights:
    root: src/rights.yaml
  security-events:
    root: src/security-events.yaml
  obligations:
    root: src/obligations.yaml
  evidence:
    root: src/evidence.yaml
  regulator-views:
    root: src/regulator-views.yaml
  consortium-roles:
    root: src/consortium-roles.yaml
  counsel-gates:
    root: src/counsel-gates.yaml
  triple-blind-exchange:
    root: src/triple-blind-exchange.yaml

extends:
  - recommended

rules:
  no-unused-components: off
  operation-4xx-response: warn
  info-license: off
  info-license-url: off
  tag-description: off
  operation-operationId: error
  no-invalid-media-type-examples: off

theme:
  openapi:
    generateCodeSamples:
      languages: []
